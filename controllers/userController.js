const { User } = require('../models')

class Controller {
    static async register (req, res) {
        try {
            
        } catch (error) {
            
        }
    }
}

module.exports = Controller